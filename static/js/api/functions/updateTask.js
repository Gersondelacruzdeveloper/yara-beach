// Import necessary modules or variables
import { csrftoken } from '../functions/crfToken.js';

// Function to open the modal for editing a task

// Function to open the modal for editing a task
function updateTask() {
    document.addEventListener('h', function() {
        const taskEdit = document.getElementById("edit-task");
        console.log('taskEdit',taskEdit)
    });
}

export {updateTask}